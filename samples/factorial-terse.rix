## Factorial — Terse Syntax ##
## Uses {? } for IF and PRINT for output ##

Fact(n) -> {? n <= 1 ? 1; n * Fact(n - 1) }

val5 = Fact(5)
val10 = Fact(10)

PRINT(
  "Factorial of 5: " + val5,
  "Factorial of 10: " + val10
)
